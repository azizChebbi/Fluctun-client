import { level } from "@features/questions/types";

export type Student = {
  bio: string;
  code: string;
  createdAt: string;
  dateOfBirth: Date;
  email: string;
  firstName: string;
  id: string;
  lastName: string;
  number: number;
  address: string;
  lastUpdatedAt: string;
  level: level;
  photo: string;
};
